name: webengage_flutter_platform_interface
description: "Platform interface package for the WebEngage Flutter SDK, providing shared
  APIs and abstractions for Android, iOS, and Web implementations."
version: 2.0.0
homepage: https://webengage.com/
repository: https://github.com/WebEngage/webengage-flutter

environment:
  sdk: ^3.5.3
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0